kind: Pod
metadata:
  namespace: dask
spec:
  restartPolicy: Never
  containers:
  - image: ulissigroup/kubeflow:predictions
    imagePullPolicy: Always
    args: [dask-worker, --nthreads, '1',--nprocs,'1', --no-dashboard, --memory-limit,'16e9',--death-timeout, '600']
    name: dask
    env:
    - name: OMP_NUM_THREADS
      value: 1
    - name: PYTHONPATH
      value: "/home/jovyan/ocp:/home/jovyan/ocp/dask-predictions"
    resources:
      limits:
        cpu: "2"
        memory: 12G
        #nvidia.com/gpu: "1"
      requests:
        cpu: "2"
        memory: 12G
        #nvidia.com/gpu: "1"
    volumeMounts:
    - mountPath: /home/jovyan
      name: workspace-zulissi
      subPath: dask-temp
    - mountPath: /home/jovyan/ocp/
      name: workspace-zulissi
      subPath: ocp/
    - mountPath: /home/jovyan/shared-datasets #Ensure that the mount path mirrors your local directory
      name: shared-datasets
    - mountPath: /home/jovyan/shared-scratch
      name: scratch-scratch
  volumes:
  - name: shared-datasets
    persistentVolumeClaim:
      claimName: shared-datasets
  - name: workspace-zulissi
    persistentVolumeClaim:
      claimName: workspace-zulissi
  - name: scratch-scratch
    persistentVolumeClaim:
      claimName: shared-scratch
      
      
      
