dask:
  partitions: -1 # if -1, 1 row per surface otherwise number of partitions

memory_cache_location: '/home/jovyan/test_cache'

output_options:
  pickle_path: test_cpu_relaxation.pkl
  pickle_folder: '/home/jovyan/shared-scratch/catlas/outputs/test_cpu_relaxation/'
  config_path: test_cpu_relaxations.yml
  parity_output_folder: 'outputs/parity-outputs/'
  verbose: false

input_options:
  bulk_file: 'catlas/bulk_structures/ocdata_bulks.db'
  adsorbate_file: 'catlas/adsorbate_structures/adsorbates.pkl'

bulk_filters:
    filter_by_mpids: ['mp-81']

adsorbate_filters:
    filter_by_smiles: ['*H']

slab_filters:
    filter_by_object_size: 100
    filter_by_max_miller_index: 1

adslab_prediction_steps:
  - step: predict
    gpu: false
    batch_size: 4
    label: 'dE_gemnet_is2re_finetuned'
    checkpoint_path: 'ocp_checkpoints/gemnet-is2re-finetuned-11-01.pt'
  - step: predict
    gpu: false
    batch_size: 4
    label: 'dE_gemnet_relax'
    checkpoint_path: 'ocp_checkpoints/gemnet_t_direct_h512_all.pt'

