kind: Pod
metadata:
  namespace: dask
spec:
  restartPolicy: Never
  containers:
  - image: ulissigroup/kubeflow:predictions
    imagePullPolicy: Always
    args: [dask-worker, --nthreads, '2',--nprocs,'1', --no-dashboard, --memory-limit,'6 GiB',--death-timeout, '600']
    name: dask
    env:
    - name: OMP_NUM_THREADS
      value: 1
    - name: PYTHONPATH
      value: "/home/jovyan/ocp:/home/jovyan/CATlas"
    resources:
      limits:
        cpu: "2"
        memory: 6G
      requests:
        cpu: "2"
        memory: 6G
    volumeMounts:
    - mountPath: /home/jovyan/ocp
      name: workspace-inference
      subPath: ocp
    - mountPath: /home/jovyan/CATlas
      name: workspace-inference
      subPath: CATlas
    - mountPath: /home/jovyan/ocp/data
      name: shared-datasets
    - mountPath: /home/jovyan/shared-scratch
      name: scratch-scratch
  volumes:
  - name: workspace-inference
    persistentVolumeClaim:
      claimName: workspace-inference
  - name: shared-datasets
    persistentVolumeClaim:
      claimName: shared-datasets
  - name: scratch-scratch
    persistentVolumeClaim:
      claimName: shared-scratch
      
      
      
